import { createApp } from 'vue'
import App from './App.vue' // Imprtando App.vue para main.js

import './assets/main.css' // Imprtando main.css para main.js

createApp(App).mount('#app') // Es el hook que se ejecuta nada más renderizar e incluir el componente en el DOM 
